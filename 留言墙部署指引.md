# 留言墙部署指引（Supabase）

> ## 🚨 先读这一段：认准 `sb_publishable_`
>
> Supabase 有**两种** key，长得像但后果天差地别：
>
> | Key | 前缀 | 能干什么 | 该放哪 |
> |---|---|---|---|
> | **Publishable** | `sb_publishable_` | 只能干 RLS 允许的事 | ✅ **发到浏览器**，公开无害 |
> | **Secret** | `sb_secret_` | **绕过所有 RLS，能删库** | ❌ 只待服务端，永远不进前端 |
>
> 老的 `service_role`（`eyJ...` 开头的长 JWT）等同于 Secret，同样不能进前端。
>
> **判断方法**：看前缀。`sb_publishable_` 才是前端要的。
>
> **万一贴错了**：立刻去 **Settings → API Keys** 找到那条 key → **⋯** → **Revoke**。
> 撤销后重建不影响表结构和数据，不用重建项目。
> 前端 `index.html` 里已经埋了哨兵 —— 填错会在控制台用红底报错并自动停用留言墙，
> 但**哨兵只能阻止请求，阻止不了已经泄露的那串字符**，所以撤 key 这一步必须手动做。

前端代码已经写完并通过测试（300 条断言 + 真实浏览器验证）。
**现在只差你把下面这段 SQL 贴进 Supabase，再把两个值填回 `index.html`。**

---

## 一、注册 + 建项目（约 3 分钟）

1. 打开 https://supabase.com ，用 GitHub 或邮箱注册（免费版不要信用卡）
2. 点 **New project**
3. 填：
   - **Name**：比如 `fzxf-wall`（避免用撇号 `'`，它在 Shell 里是特殊字符）
   - **Database Password**：点 Generate 生成一个，**存到密码管理器**（这个不是前端用的 key，别和 Publishable key 搞混）
   - **Region**：⚠️ **务必选 `Singapore`**
     这是给国内访问加速的关键。选美国的话 API 延迟会到 800ms+，留言墙会明显卡。
     项目一旦创建**不能改区域**，只能重建，所以这一步别选错。
   - **GitHub（可选）**：**留空，不要绑仓库**。那是另一套「Supabase 按你仓库里的 SQL 文件管表」的工作流，
     和我们手动跑 SQL 会打架。
   - **自动暴露新表**：**不勾**。勾上会让所有新表默认带上 anon 权限，绕过我们配的 RLS。
   - **启用自动 RLS**：**勾上**。和上一条合起来就是「默认拒绝 + 自己开权限」的正确姿势。
4. 点 **Create new project**，等 2 分钟左右（界面会显示 Initializing）


---

## 二、建表 + 配权限（复制粘贴，约 2 分钟）

左侧菜单 **SQL Editor** → **New query** → 把下面**整段**贴进去 → 点 **Run**。

```sql
-- ============================================================
--  反诈先锋 · 留言墙
-- ============================================================

create table if not exists public.messages (
  id          bigserial primary key,
  created_at  timestamptz   not null default now(),
  nickname    text          not null default '',
  content     text          not null,
  score       int,
  identity    text,
  client_fp   text          not null default '',
  is_hidden   boolean       not null default false
);

-- 常用查询：只取未隐藏的、按时间倒序
create index if not exists messages_visible_idx
  on public.messages (created_at desc)
  where is_hidden = false;

-- 指纹限流用
create index if not exists messages_fp_idx
  on public.messages (client_fp, created_at desc);

-- ============================================================
--  访问控制：两层，缺一不可
--  RLS 决定「哪些行能碰」，grant 决定「哪些操作能做」。
--  少了任何一层，数据库就是对全互联网开放的。
-- ============================================================
alter table public.messages enable row level security;

-- 先收干净：anon 什么都不能做
revoke all on public.messages from anon, authenticated;

-- 只给两个最小权限：读、写。**不 grant delete、不 grant update**。
-- 没有 delete 权限 → 前端即使被改也删不掉任何数据。
-- 没有 update 权限 → 「先发一条正常留言、过两天改成广告」这条路也堵死。
grant select, insert on public.messages to anon;

-- 登录用户（将来你若做后台登录）也只给读
grant select on public.messages to authenticated;

-- 序列（bigserial 的 id 需要它，否则 insert 会报 permission denied）
grant usage, select on sequence public.messages_id_seq to anon;


-- ---------- 策略 1：所有人都能看未隐藏的留言 ----------
drop policy if exists "public can read visible" on public.messages;
create policy "public can read visible"
  on public.messages for select
  to anon, authenticated
  using (is_hidden = false);
  -- ⚠️ 注意这里**没有** is_hidden=true 的例外：被隐藏的留言连自己都查不到，
  --    前端拿不到 = 真正生效。隐藏后要复查就去 Supabase 的 Table Editor 看。

-- ---------- 策略 2：任何人都能插入，但字段必须合规 ----------
-- 刻意**不建** delete 策略、**不建** update 策略、**不用** for all。
drop policy if exists "anon can insert" on public.messages;
create policy "anon can insert"
  on public.messages for insert
  to anon
  with check (
    is_hidden = false                                   -- 不能自己把留言设成隐藏
    and char_length(content) between 1 and 200
    and char_length(nickname) <= 12
    and (score is null or (score >= 0 and score <= 100))
    and (identity is null or identity in ('youth','mid','elder'))
    and client_fp <> ''
  );


-- ============================================================
--  发布走 RPC：限流必须在数据库侧
--  前端那个 30 秒冷却改一下 localStorage 就绕过了，只有这里算数。
-- ============================================================
create or replace function public.post_message(
  nickname  text,
  content   text,
  score     int,
  identity  text,
  client_fp text
) returns void
language plpgsql
security definer            -- 以函数属主身份插入，anon 无需直接插表权限
set search_path = public
as $$
declare
  recent    int;
  total_day int;
begin
  if char_length(coalesce(content,'')) < 1 or char_length(content) > 200 then
    raise exception '内容长度必须是 1~200 字' using errcode = '22023';
  end if;
  if length(coalesce(client_fp,'')) < 4 then
    raise exception '缺少客户端标识' using errcode = '22023';
  end if;

  -- 同一个指纹 10 秒内最多 1 条
  -- ⚠️ 必须用表别名 m 明确限定列：函数参数和表列都叫 client_fp，
  --    PL/pgSQL 默认 variable_conflict=error 时会直接抛
  --    42702「column reference "client_fp" is ambiguous」，
  --    后果是**任何合法留言都写不进去**。而且很能骗人 ——
  --    空内容那条校验在它之前就 raise 了，看起来「服务端校验正常」。
  select count(*) into recent from public.messages m
   where m.client_fp = post_message.client_fp
     and m.created_at > now() - interval '10 seconds';
  if recent >= 1 then
    raise exception '发得太快了，10 秒后再试' using errcode = 'P0001';
  end if;

  -- 同一个指纹一天最多 5 条（同样必须限定列，理由见上）
  select count(*) into total_day from public.messages m
   where m.client_fp = post_message.client_fp
     and m.created_at > now() - interval '1 day';
  if total_day >= 5 then
    raise exception '今天留言有点多，明天再来' using errcode = 'P0001';
  end if;

  -- values 里的形参一律加 post_message. 前缀，彻底消除与列名同名的歧义
  insert into public.messages (nickname, content, score, identity, client_fp)
  values (
    left(coalesce(nullif(trim(post_message.nickname),''), ''), 12),
    left(trim(post_message.content), 200),
    post_message.score, post_message.identity, post_message.client_fp
  );
end;
$$;

-- 只允许 anon 调这个函数（不给 authenticated，将来做后台再说）
revoke all on function public.post_message(text,text,int,text,text) from public;
grant execute on function public.post_message(text,text,int,text,text) to anon;
```

跑完应该看到 **Success. No rows returned**。

---

## 三、拿两个值（约 1 分钟）

**值 1：Project URL**

它在 **Settings → General**（不是 API Keys 页），往下滚到 **Project URL** 那一栏。
也可以直接看浏览器地址栏：`supabase.com/dashboard/project/<ref>/...` 里的 `<ref>` 就是项目 ID，
URL 就是 `https://<ref>.supabase.co`。

**值 2：Publishable key**

在 **Settings → API Keys**，**Publishable key** 那一栏，`sb_publishable_` 开头。

> ⚠️ 同一页下面还有 **Secret keys** 那一栏，**不要碰**。
> 它的前缀是 `sb_secret_`，能绕过所有 RLS、能删库。文件开头那张对照表就是为它写的。

> 找不到 Publishable key？老项目可能显示的是 `anon` / `public` 那一栏的
> **anon public** key（`eyJ...` 开头的长串）。Supabase 从 2025 年起改用新的
> `sb_publishable_` 格式，旧 key 年底会停用。新项目直接用 Publishable。

---

## 四、填回代码

打开 `index.html`，找到这两行：

```js
const SB_URL=window.__SB_URL || 'https://kgcfzayorjkotjmhbhpj.supabase.co';
const SB_KEY=window.__SB_KEY || 'sb_publishable_...';
```

改成你自己的值：

```js
const SB_URL=window.__SB_URL || 'https://你的ref.supabase.co';
const SB_KEY=window.__SB_KEY || 'sb_publishable_你的key';
```

`SB_URL` **结尾不要带斜杠**。

> 代码里保留了 `window.__SB_URL ||` 这一层：这样测试能在运行时注入假值验证「已配置/未配置」两条路径，
> 也方便你在控制台临时换项目（先执行 `window.__SB_URL='...'` 再刷新）。默认值就是你现在的项目。

填完把 `index.html` 推上去就行。

> **填错 key 的保护**：`index.html` 里埋了一段哨兵 —— 如果 SB_KEY 里出现了
> `sb_secret_` / `service_role` / 老版 `eyJ` 开头的 JWT，控制台会立刻用红底大字报错，
> 并自动停用留言墙（`wallReady()` 返回 false，一个请求都不发）。
> 但它**只能阻止请求，阻止不了已经泄露的那串字符** —— 所以万一填错，撤销 key 这步仍要手动做。

---

## 五、跑一遍后端自检（**别跳过**）

用浏览器打开同目录下的 **`后端自检.html`**，点「开始检查」。它会连你的 Supabase 跑 5 项：

| # | 检查 | 期望 |
|---|---|---|
| 1 | 能读到留言列表 | HTTP 200 |
| 2 | **删除必须被拒绝** ⚠️ | **HTTP 401/403** |
| 3 | 改留言也必须被拒绝 | HTTP 401/403 |
| 4 | 发布函数存在且校验生效 | HTTP 400 |
| 5 | 端到端发布一条真实留言 | 成功并回读 |

**第 2 项是重中之重。** 它是「前端没有删除能力」这个设计的唯一验证手段。

- 返回 **401/403** → ✅ 权限收对了
- 返回 **204/200** → 🚨 **立刻停下**。说明数据库对全互联网开放删除权限，
  任何人打开 F12 就能清空你的留言墙。回去重跑第二节的 SQL，
  重点确认 `revoke all on public.messages from anon, authenticated;` 成功执行了。

第 5 项会真往数据库写一条测试留言。跑完可在 **Table Editor** 里按
`client_fp = selfcheck0001` 找到它，或直接看昵称「自检」的那条，删掉即可。

自检页本身也经过验证（10 条断言，覆盖「DELETE 竟然成功」「建表 SQL 没跑」「断网」等场景），
它的判定逻辑是可靠的 —— 但你也可以自己按 F12 手动跑一遍下面这段做交叉确认。

---

## 五之二、手动交叉验证（可选，但建议做一次）

打开线上页面，F12 进 Console，贴：

```js
fetch(SB_URL + '/rest/v1/messages?id=gt.0', {
  method: 'DELETE',
  headers: { apikey: SB_KEY, Authorization: 'Bearer ' + SB_KEY }
}).then(r => console.log('状态码:', r.status));
```

**必须看到 401 或 403。** 如果返回 204 或 200，按上面第 2 项的处理方式停下重配。

顺手再验一下读：

```js
fetch(SB_URL + '/rest/v1/messages?select=*&limit=5', {
  headers: { apikey: SB_KEY, Authorization: 'Bearer ' + SB_KEY }
}).then(r => r.json()).then(console.log);
```

应该返回 `[]`（还没人留言）或几条留言对象，**不应该**出现任何 `is_hidden: true` 的记录。


---

## 六、日常维护

**删留言 / 隐藏留言**：都在 Supabase 后台做，左侧 **Table Editor** → `messages` 表。

- **想让它消失但留证据**：把 `is_hidden` 勾成 `true`。前端立刻查不到（因为策略里写死了 `is_hidden = false`），数据还在，事后可查。
- **想彻底删掉**：选中那行 → 右键 → Delete row。**这个操作只有你在后台能做**，前端没有任何删除能力。

建议每周花两分钟翻一眼 Table Editor。关键词过滤只能挡最脏的那一类，
变体（「加 薇」「V：xin」）它挡不住，**人工兜底是最后一道也是唯一可靠的防线**。

**免费版注意**：Supabase 免费项目**连续 7 天没有请求会被暂停**（数据不丢，登录后台点一下就能恢复）。
留言墙这种低流量应用有可能会触发。如果你发现页面报「留言加载失败」，
先去 Supabase 后台看一眼项目是不是被 pause 了。

---

## 附：为什么不做「只有作者能删自己的留言」

因为做不到 —— 而这正是当初和你确认过的那两条路的由来。

纯前端没有任何可靠的身份凭据：想在浏览器里判断「你是这条留言的作者」，
就得在前端存一个密码或令牌，而**前端存的东西必然是公开的**（打开 F12 就能看），
任何人抄走都能删别人的留言。这不是实现粗糙，是原理上不可能。

所以选了现在的 A + C：
- **A 后台删** —— 真正的删除能力只在 Supabase 后台，前端完全没有
- **C `is_hidden` 隐藏** —— 软删除，随时可撤销、可复查

日常的"删广告"就靠这两条，一分钟的操作。
